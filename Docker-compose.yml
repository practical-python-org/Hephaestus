---
name: hephaestus
services:

  bot:
    container_name: Hephaestus-bot
    environment_file:
      - .env

    build:
      context: bot/.
      dockerfile: Dockerfile


#  docker compose up -d --no-deps --build stale-pr-alerts cat-fact